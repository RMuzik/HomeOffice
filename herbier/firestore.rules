rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Chaque utilisateur ne peut lire/écrire que dans son propre sous-dossier.
    match /users/{userId}/fleurs/{fleurId} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
